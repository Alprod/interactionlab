<li class="w-full bg-slate-100 col-span-1 flex border-transparent shadow-lg rounded-md dark:bg-slate-600">
	<div class="flex-1 flex items-center justify-between rounded-lg truncate">
		<div class="flex-1 px-4 py-2 text-sm truncate prose">
			{% include 'includes/_start_rate.html.twig' with { idGrade:'rate_profil_star', scoreGrade:feedback.grade, class:'profil_page', size_start:'star_profil' } %}
			<div class=''>
				<p class="text-gray-900 font-medium dark:text-gray-300">Commentaire :</p>
				<p id='feedback-comment' class="text-gray-900 font-medium dark:text-gray-300">{{ feedback.comment }}</p>
			</div>
			<p class="text-sm text-gray-500/50 dark:text-white/25 mt-3"><span class='font-extrabold'>{{ feedback.createdAt|date("d/m/Y", "Europe/Paris") }}</span> | {{ fullname }}</p>
		</div>
	</div>
</li>